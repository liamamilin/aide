[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta:__legacy__"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["F", "E", "W", "I", "N"]
ignore = ["N999", "N802", "N806"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
