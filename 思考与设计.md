# AI 桌面助手 —— 设计与思考

> 最后更新：2026-05-23（开发计划追加：2026-05-23）

## 当前形态

单一悬浮窗 + 全局快捷键：
- 选中文字 → `⌘⌃L` → 自动粘贴到对话窗口 → 用户 Enter 发送
- 3 个 Agent：代码专家 / 翻译 / 通用助手
- 支持 thinking 过程实时展示
- 跨 macOS Space 和物理屏幕

## v1 原始约束（仍适用）

1. 不依赖应用类型（Terminal / VS Code / 浏览器 / PDF 都行）
2. 以“选中”作为意图入口
3. 本地 LLM（Ollama），不上传数据

## 已废弃的设计

以下来自原始设计文档，已在重构中移除：

- ~~4 Profile × 4 快捷键~~ → 1 个快捷键，通过 Agent ComboBox 切换模式
- ~~菜单栏图标 + Popover 面板~~ → 悬浮按钮 + 对话窗口
- ~~单次分析 /api/generate~~ → 多轮对话 /api/chat
- ~~Semantic Router 文本分类~~ → 用户自行选择 Agent
- ~~无状态 / 无记忆~~ → SQLite 多轮对话持久化

## 设计原则

- 极简交互：一步到达输入框，Enter 即发
- 本地优先：Ollama 本地推理，不上传文本
- macOS 原生感：悬浮窗、Space 跟随、剪贴板安全
- 能力正交：Agent 间行为差异由 prompt 约束，不引入额外逻辑

## 已知问题 / 故障排除

### macOS 辅助功能权限

快捷键弹窗但内容为空，日志出现 `This process is not trusted!` 或 `Clipboard unchanged after Cmd+C`：

- **原因**：pynput 的 `CGEventPost` 需要辅助功能权限才能模拟按键
- **解决**：系统设置 → 隐私与安全性 → 辅助功能 → 勾选终端应用（Terminal.app / iTerm.app），必要时取消重勾刷新 TCC 缓存
- **代码策略**：pynput → osascript 双回退，但两者都需要辅助功能权限
- 参见 2026-05-23 定位记录：根目录 vs 项目目录表现不同，根因是 pyenv shim 解析路径导致 TCC 权限不匹配

## 开发路线图

### 🔧 P0 — 立即可修

- [x] P0-1 模型下拉样式修复（hover 白字 + 文字截断）
- [ ] P0-2 快捷键触发后自动聚焦输入框（当前需鼠标点击）
- [ ] P0-3 助手回复气泡增加「复制」按钮

### ⚡ P1 — 核心体验完善

- [ ] P1-1 对话历史浏览（SQLite 已存数据，缺浏览/加载 UI）
- [ ] P1-2 暗色模式（跟随 macOS 系统主题，当前硬编码亮色）
- [ ] P1-3 错误状态优化（Ollama 不可用时提前告知，而非点发送后才报错）
- [ ] P1-4 输入框快捷键（Esc 清空、Shift+Enter 换行、Ctrl+N 新建对话）

### 🔨 P2 — 功能增强

- [ ] P2-1 配置面板 UI（模型地址、快捷键、Agent 编辑，替代手动改 config.py）
- [ ] P2-2 Agent 自定义（增删改 Agent 及 prompt 模板）
- [ ] P2-3 导出对话（复制整段对话为 Markdown / 纯文本）
- [ ] P2-4 启动时恢复上次对话

### 📋 P3 — 远期考虑

- [ ] P3-1 菜单栏图标入口（悬浮按钮的替代/补充）
- [ ] P3-2 图片输入（Ollama 多模态模型支持）
- [ ] P3-3 自动化测试（UI / 集成测试）
- [ ] P3-4 对话搜索（全文搜索历史对话）
