# AI 桌面助手 —— 设计与思考

> 最后更新：2026-05-26（开发完成）

## 当前形态

三入口 + Agent 多轮对话：
- **菜单栏图标**：macOS 菜单栏常驻，左键打开对话，右键快速切换 Agent / 设置 / 退出
- **悬浮按钮**：屏幕右侧圆形图标，左键开关对话，右键菜单（设置、关于等），跨 Space 跟随
- **全局快捷键** `⌘⌃L`：选中 → 按快捷键 → 自动填入对话窗口 → Enter 发送
- Agent 系统：5 个内置 + 自定义 Agent（新增/编辑/删除），自定义 prompt 和 emoji 图标
- 流式输出 + thinking 过程折叠展示 + Markdown 渲染
- SQLite 持久化：对话历史、设置、自定义 Agent
- 历史浏览：搜索 + 加载 + 删除
- 对话导出：一键复制为 Markdown

## v1 原始约束（仍适用）

1. 不依赖应用类型（Terminal / VS Code / 浏览器 / PDF 都行）
2. 以"选中"作为意图入口
3. 本地 LLM（Ollama），不上传数据

## 已废弃的设计

以下来自原始设计文档，已在重构中移除：

- ~~4 Profile × 4 快捷键~~ → 1 个快捷键 + Agent ComboBox + 菜单栏切换
- ~~菜单栏图标 + Popover 面板~~ → 菜单栏图标 + 悬浮按钮 + 对话窗口
- ~~单次分析 /api/generate~~ → 多轮对话 /api/chat
- ~~Semantic Router 文本分类~~ → 用户自行选择 Agent（已实现自定义 Agent）
- ~~无状态 / 无记忆~~ → SQLite 多轮对话持久化

## 设计原则

- 极简交互：一步到达输入框，Enter 即发
- 本地优先：Ollama 本地推理，不上传文本
- macOS 原生感：菜单栏图标、悬浮窗、Space 跟随、剪贴板安全
- 能力正交：Agent 间行为差异由 prompt 约束，不引入额外逻辑

## 已知问题 / 故障排除

### macOS 辅助功能权限

快捷键弹窗但内容为空，日志出现 `This process is not trusted!` 或 `Clipboard unchanged after Cmd+C`：

- **原因**：pynput 的 `CGEventPost` 需要辅助功能权限才能模拟按键
- **解决**：系统设置 → 隐私与安全性 → 辅助功能 → 勾选终端应用（Terminal.app / iTerm.app）
- **代码策略**：pynput CGEvent → osascript 双回退；延迟 100ms 避免修饰键冲突

### 输入法干扰

在某些编辑场景（中文输入法激活时），快捷键可能不触发。疑似 IME 拦截 `⌃L`。快速按下可规避。

## 开发路线图

### ✅ P0 — 已修复

- [x] P0-1 模型下拉样式修复
- [x] P0-2 快捷键触发后自动聚焦输入框
- [x] P0-3 助手回复气泡增加「复制」按钮

### ✅ P1 — 已完成

- [x] P1-1 对话历史浏览（SQLite 已存数据，缺浏览/加载 UI）
- [x] ~~P1-2 暗色模式~~（跳过，约定不实现）
- [x] P1-3 错误状态优化（Ollama 连接指示灯，每 30s 探活）
- [x] P1-4 输入框快捷键（Esc 两段式清空/关闭、Ctrl+N 新建、Shift+Enter 防误发）

### ✅ P2 — 已完成

- [x] P2-1 配置面板 UI（运行时可改：Ollama 地址、超时、上下文窗口、快捷键）
- [x] P2-2 Agent 自定义（新增/编辑/删除，emoji 选择器，JSON 持久化）
- [x] P2-3 导出对话（复制整段为 Markdown 到剪贴板，闪 ✅ 反馈）
- [x] P2-4 启动时恢复上次对话（首次打开自动加载最新对话）

### ⚡ P3 — 部分完成

- [x] P3-1 菜单栏图标（QSystemTrayIcon，Agent 切换 + 对话开关）
- [ ] ~~P3-2 图片输入~~（跳过，本地多模态模型少，价值低）
- [x] P3-3 自动化测试（110 项：storage CRUD、chat stream、markdown、normalizer、管理器、UI 信号/状态、剪贴板、快捷键）
- [x] P3-4 对话搜索（SQL LIKE 标题+内容，300ms 防抖）

### ✨ 优化增强（已完成）

- [x] 异步 HTTP 探活（_OllamaPingWorker QThread）
- [x] 关键 slot try/except 防护
- [x] config.py 类型清理（typing.List → list）
- [x] Agent 持久化修复（自动恢复不覆盖用户选择）
- [x] 流式中断按钮（⏹）
- [x] 消息编辑按钮（✏️ 用户消息 hover）
- [x] macOS 通知推送（回复完成时，窗口在后台）
- [x] osascript 回退路径修复
- [x] float_button mouseReleaseEvent super() 修复
- [x] 3 处未使用 import 清理

**完成度：13/14 + 10 项优化增强。项目功能完整。**

---

## 已知技术债务

以下为代码审查发现的已知问题，已记录供后续迭代参考（2026-05-26）：

| # | 问题 | 影响 | 优先级 |
|---|------|------|--------|
| 1 | **测试覆盖已补强** — 已引入 pytest-qt 覆盖核心 UI 信号/状态，仍需 CI 自动化执行 | 本地已有保护，远端合并仍缺自动门禁 | 中 |
| 2 | **UI 硬编码尺寸** — 440×580px，小屏（MacBook Air 13"）可能溢出，无自适应 | 小屏用户使用受限 | 中 |
| 3 | **`read_selection()` 主线程 sleep** — 80ms 阻塞 UI 事件循环 | 短暂卡顿，几乎无感但不应存在 | 低 |
| 4 | **slot 缺乏统一错误边界** — try/except 是后来补的，非架构级防御 | 新开发容易遗漏，回归风险 | 中 |

## 后续功能计划

以下为可考虑的功能增强（按优先级）：

### 🔧 技术改进

- [x] UI 测试框架引入（pytest-qt）
- [ ] 窗口最小尺寸改为动态（`QScreen.availableGeometry()` 百分比）
- [ ] `read_selection()` 改为异步 / QTimer 链式回调

### ✨ 功能增强

- [ ] 对话重命名（手动编辑标题）
- [ ] 流式完成时可选自动弹出窗口
- [ ] 快捷键冲突检测（启动时检测 `⌘⌃L` 是否被占用）
- [ ] 开机自启（LaunchAgent plist）

### 🧹 体验优化

- [ ] 首次启动引导（未检测到 Ollama 时的友好提示）
- [ ] `aide` 命令行参数（`--hide-float`、`--no-tray` 等）
- [ ] 窗口位置/大小记忆（记住上次关闭时的状态）
