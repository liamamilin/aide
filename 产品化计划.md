# AI 桌面助手 — 产品化计划

> 当前状态：完整可用的个人工具（v1）。从个人工具到可分发产品，需要补齐以下环节。

## 产品化路线图

### 阶段 1: 打包与分发（预计 2-3 天）

**目标**：不装 Python 也能用，双击打开。

| 任务 | 说明 | 技术 |
|------|------|------|
| `.app` 打包 | 将 Python + Qt + 依赖打包为 macOS 原生 `.app` | `py2app` 或 `PyInstaller` |
| 内嵌 Python 运行时 | 用户无需安装 pyenv / pip | 打包工具自动处理 |
| 代码签名 | macOS Gatekeeper 要求签名否则弹警告 | `codesign` + Apple Developer ID |
| 公证 (Notarization) | macOS 15+ 要求公证否则无法打开 | `xcrun notarytool` |
| DMG 分发 | `.dmg` 拖拽到 Applications 的标准安装方式 | `create-dmg` |
| 版本号管理 | 语义化版本 `v1.0.0`，自动注入构建 | `setuptools_scm` |

**里程碑**：用户下载 DMG → 拖入 Applications → 双击打开 → 正常运行。

---

### 阶段 2: 首次体验（预计 1 天）

**目标**：新用户 3 分钟内完成首次问答。

| 任务 | 说明 |
|------|------|
| **Ollama 引导** | 启动时检测 Ollama 是否运行，如果未运行 → 友好引导页（"请先安装 Ollama" + 下载链接 + 拉取命令） |
| **默认模型自动拉取** | 检测模型是否已拉 → 未拉则显示 `ollama pull` 进度 |
| **权限引导** | 检测辅助功能权限 → 未授权则打开系统设置页 |
| **欢迎窗口** | 首次启动显示简短介绍：三个入口、三个 Agent、快捷键 |

**里程碑**：新用户首次启动 → 看到引导 → 装好依赖 → 完成首次问答。

---

### 阶段 3: 体验打磨（预计 2 天）

**目标**：从"能用"到"舒服"。

| 任务 | 说明 |
|------|------|
| **正式图标** | 当前 `图标.png` 是临时素材，需 macOS 风格圆角图标 `.icns` |
| **暗色模式** | 跟随 macOS 系统主题（P1-2，之前跳过） |
| **窗口自适应** | 当前硬编码 440×580，改为按屏幕百分比计算 |
| **错误边界** | 全局异常捕获，弹友好错误提示而非静默崩溃 |
| **更新检查** | 启动时检查 GitHub Releases 是否有新版本 |

**里程碑**：看起来像认真的 macOS 应用。

---

### 阶段 4: 质量保障（预计 2 天）

**目标**：不怕改。

| 任务 | 说明 |
|------|------|
| **UI 测试** | `pytest-qt` 覆盖核心交互：发送消息、切换 Agent、加载历史 |
| **CI/CD** | GitHub Actions：lint + 110 项现有测试 |
| **崩溃收集** | 接入 Sentry 或本地崩溃日志聚合 |

**里程碑**：PR 合并前自动跑全量测试。

---

### 阶段 5: 发布与迭代（持续）

| 任务 | 说明 |
|------|------|
| **GitHub Releases** | 每次发版附 DMG + Changelog |
| **Homebrew Cask** | `brew install --cask ai-desktop-assistant` |
| **自动更新** | Sparkle 框架，检测新版本 → 下载 → 替换 |
| **用户反馈渠道** | GitHub Issues 或 Discord |

---

## 不做的事情（明确边界）

- ❌ 不上架 Mac App Store（Ollama 依赖、沙盒限制）
- ❌ 不支持 Windows / Linux（macOS 原生 API 依赖太重）
- ❌ 不接云端 LLM（v1 核心约束：数据不上传）
- ❌ 不收费（个人项目，开源 MIT）

---

## 时间估算

| 阶段 | 工作量 |
|------|--------|
| 1 — 打包分发 | 2-3 天 |
| 2 — 首次体验 | 1 天 |
| 3 — 体验打磨 | 2 天 |
| 4 — 质量保障 | 2 天 |
| 5 — 发布迭代 | 持续 |
| **合计** | **约 8 天** |

---

## 参考：当前已完成

项目目前已具备：5 个内置 Agent + 自定义、流式对话 + 思考可视化、历史搜索、对话导出、运行时设置、3 入口（快捷键/悬浮/菜单栏）、110 项自动化测试。从功能角度看已完整，以上是"产品化"而非"功能补全"。
